import{_ as e,o as a,c as o,e as d}from"./app-no34rlVp.js";const t={},r=d(`<h1 id="ddex" tabindex="-1"><a class="header-anchor" href="#ddex" aria-hidden="true">#</a> DDEX</h1><h2 id="what-is-ddex" tabindex="-1"><a class="header-anchor" href="#what-is-ddex" aria-hidden="true">#</a> What is DDEX?</h2><p>DDEX is a standards setting organisation focused on the creation of digital value chain standards to make the exchange of data and information across the music industry more efficient.</p><p>OWN3D supports the ERN (Electronic Release Notification) standard, for versions <code>3.8.2</code> and <code>4.1</code>.</p><p>Our Party ID is:</p><pre><code>PA-DPIDA-2021121507-Q
</code></pre><h2 id="ftp-accounts" tabindex="-1"><a class="header-anchor" href="#ftp-accounts" aria-hidden="true">#</a> FTP Accounts</h2><p>OWN3D supports the DDEX format and allows uploads through the FTP protocol. Credentials can be requested through your OWN3D Partner Manager.</p><p>Server: <code>ddex.partners.jukebox.gg</code></p><h2 id="ftp-folder-structure" tabindex="-1"><a class="header-anchor" href="#ftp-folder-structure" aria-hidden="true">#</a> FTP Folder Structure</h2><p>A complete upload includes one <code>batch folder</code> and several <code>package folders</code>.<br> A <code>batch</code> is a collection of <code>packages</code> arranged into a particular directory structure.<br> The <code>batch folder</code> consists of a date and a serial number.<br> A <code>package</code> is a folder containing all these assets and the metadata for each album.</p><h2 id="file-formats" tabindex="-1"><a class="header-anchor" href="#file-formats" aria-hidden="true">#</a> File Formats</h2><p>Album Cover files can be uploaded in <code>.jpg</code> formats and must be at least <code>500×500 pixels</code>.<br> Audio files can be uploaded in <code>.wav</code> or <code>.flac</code>, lossless formats.<br> XML metadata files should be <code>UTF-8</code> encoded.<br> We follow the ERN (Electronic Release Notification) standard, for versions <code>3.8.2</code> and <code>4.1</code>.</p>`,13),c=[r];function s(n,i){return a(),o("div",null,c)}const h=e(t,[["render",s],["__file","ddex.html.vue"]]);export{h as default};
